name: C/C++ CI

on:
  push:
    branches: [ "master" , "develop"]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Display event payload
      run: |
        echo "Hello World"
      
 
